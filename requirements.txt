# Pinned to the versions validated locally (Python 3.12) ahead of the
# National Rural STEM Learning Summit. Relax to >= after the conference.
streamlit==1.57.0
anthropic==0.100.0
pydantic==2.13.4
plotly==6.7.0
networkx==3.6.1
numpy==2.4.4
